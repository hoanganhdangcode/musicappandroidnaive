<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicApp"
        android:usesCleartextTraffic="true" >
        <activity
            android:name=".views.User.PlayMusicActivity"
            android:exported="false" />
        <activity
            android:name=".views.Admin.DashboarđAminActivity"
            android:exported="false"
            android:label="@string/title_activity_dashboarđ_amin"
            android:theme="@style/Theme.MusicApp" />
        <activity
            android:name=".views.Admin.AdminMainActivity"
            android:exported="false" />
        <activity
            android:name=".views.User.UserMainActivity"
            android:exported="false" />
        <activity
            android:name=".views.SplashActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.SignupActivity"
            android:exported="false" />
        <activity
            android:name=".views.LoginActivity"
            android:exported="false" />
    </application>

</manifest>